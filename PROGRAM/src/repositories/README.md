# Repositories (Data Access Layer)

This folder contains repository implementations responsible for data persistence.

- Use **parameterized queries** (or a query builder) to prevent SQL injection.
- Keep SQL details here; do not leak into services/controllers.

